const chai = require('chai');
const getUrls = require('../src/get-links.js')

describe('Validar Links', function() {
  it('Debe comprobar si existe patron []()', () => {
    chai.assert.equal('')
  })
  it('Debe comprobar si es una url valida', () => {
    chai.assert.equal('')
  })
})